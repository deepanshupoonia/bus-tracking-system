import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { useAuth } from '../context/AuthContext.jsx';
export function LoginPage() { const {login}=useAuth();const navigate=useNavigate();const [form,setForm]=useState({email:'student@college.test',password:'College@123'}),[error,setError]=useState(''); const submit=async(e)=>{e.preventDefault();try{await login(form);navigate('/');}catch(err){setError(err.response?.data?.message??'Could not sign in.');}};return <main className="auth"><form onSubmit={submit} className="panel"><p className="eyebrow">College Bus Tracking</p><h1>Welcome back</h1><p className="hint">Student demo: student@college.test<br/>Driver demo: driver@college.test</p><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{error&&<p className="error">{error}</p>}<button>Sign in</button></form></main>; }
