import { Router } from 'express'; import { current,end,location,start } from '../controllers/driver.controller.js'; import { authenticate,authorize } from '../middleware/auth.js'; export const driverRouter=Router(); driverRouter.use(authenticate,authorize('DRIVER')); driverRouter.get('/current-route',current); driverRouter.post('/start-route',start); driverRouter.post('/end-route',end); driverRouter.post('/location',location);
